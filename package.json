{
    "name": "homex",
    "version": "2.0.0",
    "description": "HomeX - Construction Materials Delivery Platform",
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "start": "cd server && node index.js",
        "install-all": "npm install --prefix client && npm install --prefix server"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}